pub mod capabilities;
pub mod core;
pub mod registrar;

//use crate::common::snitches::TransactionEmptySnitch;
//use sip_server::{Core, SipBuilder, SipManager, Transport};
//use std::sync::Arc;

/*
async fn setup() -> Arc<SipManager> {
    SipBuilder::new::<Core, TransactionEmptySnitch, Transport>()
        .expect("sip manager failed")
        .manager
}*/
